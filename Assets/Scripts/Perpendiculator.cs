using UnityEngine;
using System.Collections;

[ExecuteInEditMode]
public class Perpendiculator : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.Rotate(Vector3.right, 90f);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
